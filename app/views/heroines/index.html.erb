<h2>All Heroines</h2>

<ul>
  <% @heroines.each do |heroine| %>
    <li>
      <%= link_to heroine.super_name, heroine_path(heroine) %>
    </li>
    <br>
  <% end %>
</ul>

<h3><%= link_to "View All Powers", powers_path %></h3>
<%= button_to "Create a Heroine", new_heroine_path, method: 'get' %>
